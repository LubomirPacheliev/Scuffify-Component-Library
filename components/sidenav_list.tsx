import React from 'react';

type Props = { width: Number, height: Number, isFlex: boolean }

const sidenav_list = (props: Props) => {
    const { width, height, isFlex } = props;
    
    return (
        <div>sidenav listing biiiitch 🤑</div>   
    )
}

export default sidenav_list;

// my balls are sweaty, knees weak, arms are heavy, imma cum in this bitch mouth already